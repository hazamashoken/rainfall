#include <stdio.h>

void v(void){
    char buf[520];

    fgets(buf, 520, stdin);

    printf(buf);
    // [...]
    int m;
    if (m == 64) {
        fwrite("Wait what?!\n", 1, 13, stdout);
        system("/bin/sh");
    }
}

int main(void) {
    v();
    return 0;
}