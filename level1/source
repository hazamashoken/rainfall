// TODO: NOT FULLY DONE

#include <stdio.h>
#include <stdlib.h>

void run(void){
    fwrite("Good... Wait what?\n", 1, 20, stdin);
    system("/bin/sh");
}

int main(void){
    char str[80];
    gets(*str);
    return (0); 
}